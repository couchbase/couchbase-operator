# The base configuration for updating indexStorageSetting tests
apiVersion: couchbase.com/v1
kind: CouchbaseCluster
metadata:
  name: cb-example
spec:
  version: enterprise-5.5.0
  authSecret: cb-example-auth
  cluster:
    indexStorageSetting: memory_optimized
  servers:
    - size: 3
      name: all_services
      services:
        - data
        - query
        - search
