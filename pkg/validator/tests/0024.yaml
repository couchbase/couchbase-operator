# The minimum base configuration
apiVersion: couchbase.com/v1
kind: CouchbaseCluster
metadata:
  name: cb-example
spec:
  version: enterprise-5.5.0
  authSecret: cb-example-auth
  cluster:
    indexStorageSetting: plasma
  buckets:
    - name: buck$et
      type: memcached
      memoryQuota: 256
      enableFlush: false
  servers:
    - size: 3
      name: all_services
      services:
        - data
        - index
        - query
        - search
