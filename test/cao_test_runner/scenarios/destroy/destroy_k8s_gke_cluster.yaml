# The following fields need to be set in env / context
# CAO_BINARY_PATH / CAOBinaryPath
# CRD_PATH / CRDPath
# CLUSTER_NAME / ClusterName
# PLATFORM / Platform
# ENVIRONMENT / Environment
# PROVIDER / Provider
# GKE_REGION / GKERegion

action: "Destroy Admission Controller"
config:
  description:
    - "destroy admission controller in GKE cluster"
trees:
  - action: "Destroy Operator"
    config:
        description:
          - "destroy operator in GKE cluster"
    trees:
      - action: "Delete CRDs"
        config:
          description:
            - "delete crds from GKE cluster"
        trees:
          - action: "Destroy Kubernetes Cluster"
            config:
              description:
                - "destroy the GKE cluster"
