# The following fields need to be set in env / context
# CAO_BINARY_PATH / CAOBinaryPath
# CRD_PATH / CRDPath
# CLUSTER_NAME / ClusterName
# PLATFORM / Platform
# ENVIRONMENT / Environment
# PROVIDER / Provider
# AKS_REGION / AKSRegion

# This yaml may also require the following env to be set
# AKS_REGION
# AKS_SUBSCRIPTION_ID
# AKS_SERVICE_PRINCIPAL_ID
# AKS_SERVICE_PRINCIPAL_SECRET_ID
# AKS_TENANT_ID

action: "Destroy Admission Controller"
config:
  description:
    - "destroy admission controller in AKS cluster"
trees:
  - action: "Destroy Operator"
    config:
        description:
          - "destroy operator in AKS cluster"
    trees:
      - action: "Delete CRDs"
        config:
          description:
            - "delete crds from AKS cluster"
        trees:
          - action: "Destroy Kubernetes Cluster"
            config:
              description:
                - "destroy the AKS cluster"
