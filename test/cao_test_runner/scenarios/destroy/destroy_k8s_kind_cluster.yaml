# The following fields need to be set in env / context
# CAO_BINARY_PATH / CAOBinaryPath
# CRD_PATH / CRDPath
# CLUSTER_NAME / ClusterName
# PLATFORM / Platform
# ENVIRONMENT / Environment

action: "Destroy Admission Controller"
config:
  description:
    - "destroy admission controller in kind cluster"
trees:
  - action: "Destroy Operator"
    config:
        description:
          - "destroy operator in kind cluster"
    trees:
      - action: "Delete CRDs"
        config:
          description:
            - "delete crds from kind cluster"
        trees:
          - action: "Destroy Kubernetes Cluster"
            config:
              description:
                - "destroy the kind cluster"
