- hosts: master_node
  vars:
    kubeConfigDir: ~/.kube

  tasks:
    - name: Copying namespace file
      copy:
        src: /tmp/namespace.json
        dest: "{{ kubeConfigDir }}/namespace.json"

    - name: Creating namespace using kubectl command
      shell: "kubectl create -f {{ kubeConfigDir }}/namespace.json"
